import 'package:flutter/material.dart';

class OutletMergeMap extends StatelessWidget {
  const OutletMergeMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
